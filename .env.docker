# WorkZone – Docker Environment Variables
# Copy this file and fill in your real values before running docker-compose.
# Do NOT commit the filled-in version to version control.

# ── Backend ────────────────────────────────────────────────────────────────
PORT=5000
NODE_ENV=production

# MongoDB connection string (use the service name "mongo" inside Docker)
MONGO_URI=mongodb://mongo:27017/workzone

# JWT signing secret – change this to a long, random string
JWT_SECRET=change-this-to-a-secure-random-secret

# Google OAuth client ID (from https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=your-google-client-id-here

# URL of the deployed frontend (used for CORS)
FRONTEND_URL=http://localhost
